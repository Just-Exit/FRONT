# Mocks

Keep typed fixture data here while no API exists. UI components should receive data through props so a future data source can replace these fixtures without rewriting presentation components.
