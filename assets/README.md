# Assets

- Keep raster images under `assets/images` and import static files with `require`.
- Add density variants with `@2x` and `@3x` when supplied by design.
- Prefer exported SVG/icon source files from Figma; choose the rendering library only after actual asset requirements are known.
- Existing icon files are Expo placeholders and should be replaced when brand assets are provided.
